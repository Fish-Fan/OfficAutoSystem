<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fanyank.mapper.IMMessageMapper">
    <insert id="insert" parameterType="com.fanyank.pojo.IMMessage" useGeneratedKeys="true">
        insert into immessage(cid,username,avatar,id,type,content,fromid,timestamp)
        values(#{cid},#{username},#{avatar},#{id},#{type},#{content},#{fromid},#{timestamp})
    </insert>
</mapper>